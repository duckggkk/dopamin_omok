# Backend
backend/build/
backend/bin/
backend/.gradle/
backend/*.log
backend/logs/
application-local.yml

# Node (루트 dev 스크립트용 concurrently 등)
/node_modules/

# Frontend
frontend/node_modules/
frontend/dist/
frontend/.env.local
frontend/.env.production

# IDE
.idea/
*.iml
.vscode/
*.DS_Store

# Env (예시 파일은 커밋, 실제 값 파일은 제외)
.env
.env.prod
*.env.local

# 운영 산출물 (서버에서 생성됨 — 커밋 금지)
/backups/
deploy/maintenance/maintenance.on

# Docker
mysql/data/

# Claude Code
.claude/

# Internal docs (설계 문서, 외부 공개 불필요)
docs/

# 개인 공부 노트 (학습용 — 저장소에 올리지 않음)
study/
