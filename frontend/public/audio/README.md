# Background music assets

Place licensed background music files here:

- `classic-omok-bgm.mp3` for classic Omok
- `physical-omok-bgm.mp3` for physical Omok

These files are served as frontend static assets, so playback does not go through the Spring backend or WebSocket server. Keep files compressed and cacheable.
